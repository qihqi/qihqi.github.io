# Cero vale todo

This repository now uses MkDocs instead of Jekyll.

## Local development

```bash
uv venv
source .venv/bin/activate
uv sync
mkdocs serve
```

## Build

```bash
source .venv/bin/activate
uv sync
mkdocs build
```
