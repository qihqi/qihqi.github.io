[project]
name = "qihqi-github-io"
version = "0.1.0"
description = "Zensical site for archived blog posts"
requires-python = ">=3.11"
dependencies = [
  "zensical>=0.0.21",
]

[tool.uv]
package = false
