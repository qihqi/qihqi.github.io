[project]
name = "qihqi-github-io"
version = "0.1.0"
description = "MkDocs site for archived blog posts"
requires-python = ">=3.11"
dependencies = [
  "mkdocs>=1.6.0",
  "mkdocs-material>=9.6.0",
]

[tool.uv]
package = false
